[package]
name = "rusty-ui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
console_error_panic_hook = "0.1.7"
js-sys = "0.3.104"
leptos = { version = "0.8.20", features = ["csr"] }
rusty-ai = { path = "../rusty-ai", default-features = false }
rusty-core = { path = "../rusty-core", default-features = false }
rusty-edit = { path = "../rusty-edit", default-features = false }
# The message catalogue. No IO, no backend half — the frontend looks a
# string up the same way the backend will when the diagnostics move over.
rusty-i18n = { path = "../rusty-i18n" }
rusty-embed = { path = "../rusty-embed", default-features = false }
rusty-ipc = { path = "../rusty-ipc" }
rusty-lsp = { path = "../rusty-lsp", default-features = false }
rusty-dbg = { path = "../rusty-dbg", default-features = false }
rusty-term = { path = "../rusty-term", default-features = false }
rusty-git = { path = "../rusty-git", default-features = false }
# A real Markdown parser, for reading a README rather than for the six
# constructs a hover tooltip uses. Pure Rust, no unsafe, compiles to wasm —
# and the hand-rolled block splitter it replaces was already wrong about
# heading levels, ordered lists, tables and nesting, all of which a project's
# own documentation is full of.
pulldown-cmark = { version = "0.13.4", default-features = false }
serde = { version = "1.0.229", features = ["derive"] }
serde-wasm-bindgen = "0.6.5"
serde_json = "1.0.151"
wasm-bindgen = "0.2.127"
wasm-bindgen-futures = "0.4.77"
web-sys = { version = "0.3.104", features = ["Window", "Document", "Element", "HtmlElement", "Storage", "MediaQueryList", "KeyboardEvent", "HtmlInputElement", "HtmlSelectElement", "DomTokenList", "CssStyleDeclaration", "Navigator", "Clipboard", "HtmlSpanElement", "HtmlDivElement", "HtmlTextAreaElement", "FocusOptions", "KeyboardEventInit", "HtmlDocument", "DomRect", "ClipboardEvent", "DataTransfer", "WheelEvent", "ResizeObserver", "ResizeObserverEntry", "HtmlCanvasElement", "CanvasRenderingContext2d", "TextMetrics", "MouseEvent", "Performance", "Selection"] }

[lints]
workspace = true
