{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to ask the OS for. Kept minimal on purpose: analysis happens in Rust, so the WebView needs no filesystem, shell, or network access of its own.",
  "windows": ["main", "edit-*"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:webview:allow-set-webview-zoom"
  ]
}
