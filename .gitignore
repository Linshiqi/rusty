# Unanchored: test fixtures are standalone workspaces and grow their own target/
target/
**/*.rs.bk
*.pdb

# Trunk output and its wasm-bindgen cache
dist/
.trunk/

# Tauri codegen
/crates/rusty-app/gen

.DS_Store
Thumbs.db

# rusty's own QEMU, fetched into the bundle by scripts/fetch-qemu.sh —
# sixty megabytes of binaries that belong in a release asset, not in git
/crates/rusty-app/bundled/qemu/
/crates/rusty-app/bundled/*.tar.xz
