# `[workspace]` on its own detaches this from rusty's workspace. Without it
# cargo refuses to build anything under a workspace root it is not a member
# of, with an error about believing it is in a workspace.
[workspace]

[package]
edition = "2024"
name    = "rate-loop"
version = "0.1.0"

[[bin]]
name = "rate-loop"
path = "./src/main.rs"

[dependencies]
esp-hal                = { version = "~1.1.0", features = ["esp32c3", "unstable"] }
esp-println            = { version = "0.15", features = ["esp32c3"] }
esp-bootloader-esp-idf = { version = "0.5.0", features = ["esp32c3"] }
critical-section       = "1.2.0"
# `atan2` and `sqrt` are `std` methods; a no_std attitude filter needs the
# real libm behind them. Tiny, and the canonical answer for embedded float.
libm                   = "0.2"

[profile.release]
codegen-units = 1
debug         = 2
lto           = "fat"
opt-level     = "s"
